# Configuração base da aplicação (pode ser sobreposta por variáveis de ambiente).

# HTTP
app.http.host=0.0.0.0
app.http.port=8080

# Endpoints API
app.graphql.path=/graphql
app.websocket.audit.path=/audit/stream

# Base de dados PostgreSQL
db.url=jdbc:postgresql://localhost:5432/elections
db.user=elections
db.password=elections
